export function SeoBar() {
  return (
    <div className="w-full border-b border-[var(--border)] bg-white px-4 py-3 sm:px-6">
      <div className="mx-auto max-w-[1600px] text-center text-sm leading-relaxed text-[var(--muted)]">
        <p>
          Compile Typst documents to PDF, SVG, PNG and HTML online — pinned compiler versions,
          Typst Universe packages, custom fonts, and a live editor playground.
        </p>
        <p className="mt-0.5">
          Free, open-source typesetting in your browser. No install required.
        </p>
      </div>
    </div>
  );
}
